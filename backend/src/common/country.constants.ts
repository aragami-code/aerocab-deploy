/** Pays de repli ultime si aucun Country.isDefault n'est défini. */
export const FALLBACK_COUNTRY = 'CM';

/** Clé app_settings de la devise de référence (ancrage des points). */
export const REFERENCE_CURRENCY_KEY = 'reference_currency';

/** Devise de référence par défaut si la clé n'est pas définie. */
export const DEFAULT_REFERENCE_CURRENCY = 'XAF';
