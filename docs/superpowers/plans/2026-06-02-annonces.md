# Système d'annonces & gestion de version APK — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre à l'admin de gérer des annonces (promo/info/pub interne) ciblées et la version des apps, diffusées via modal au lancement, centre de notifications fusionné, et gate de version APK avec téléchargement/installation in-app.

**Architecture:** Approche A (poll au lancement). Le backend expose un feed filtré côté serveur ; les apps mobiles l'appellent au démarrage + retour 1er plan (cache 5 min). Version stockée en `app_settings`. Centre de notifs fusionné (push local + annonces serveur).

**Tech Stack:** NestJS + Prisma (backend), React/Vite (admin), Expo/React Native + Zustand (mobile), `expo-file-system` + `expo-intent-launcher` (download APK).

**Spec de référence:** `docs/superpowers/specs/2026-06-02-annonces-design.md`

---

## File Structure

**Backend (`aerocab-deploy/backend`)**
- `prisma/schema.prisma` — modèles `Announcement` + `AnnouncementRead` + enums
- `prisma/seed-rbac.ts` — ajoute permission `manage_announcements`
- `src/announcements/announcements.module.ts` — module
- `src/announcements/announcements.service.ts` — logique (filtrage feed, CRUD, version)
- `src/announcements/announcements.controller.ts` — endpoints mobile
- `src/announcements/announcements.admin.controller.ts` — endpoints admin
- `src/announcements/dto/*.ts` — DTOs
- `src/announcements/announcements.service.spec.ts` — tests unitaires

**Admin (`aerocab-admin`)**
- `src/pages/AnnoncesPage.tsx` — page CRUD + onglet version
- `src/services/api.ts` — méthodes API (modif)
- `src/App.tsx` + `src/components/Sidebar.tsx` — route + nav (modif)

**Mobile (passager + chauffeur, identique)**
- `services/api.ts` — `getAnnouncementsFeed`, `markAnnouncementSeen` (modif)
- `stores/announcementsStore.ts` — cache feed + version
- `components/AnnouncementModal.tsx` — modal au lancement
- `components/UpdateGate.tsx` — gate version + download in-app
- `app/(tabs)/notifications.tsx` — fusion annonces dans l'inbox (modif)
- `app/_layout.tsx` — montage `UpdateGate` + `AnnouncementModal` (modif)
- `app.config.js` — permission `REQUEST_INSTALL_PACKAGES` (modif)

---

# PHASE 1 — BACKEND

### Task 1 : Modèles Prisma `Announcement` + `AnnouncementRead`

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Step 1 : Ajouter les enums et modèles**

Ajouter à la fin de la section enums (après `enum BookingType`) :

```prisma
enum AnnouncementType {
  promo
  info
  ad
}

enum AnnouncementPriority {
  high     // modal au lancement
  normal   // centre de notifs uniquement
}

enum AnnouncementCtaType {
  none
  screen
  promo_code
}
```

Ajouter les modèles (avant la première ligne `enum` ou à la fin des models) :

```prisma
model Announcement {
  id              String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  type            AnnouncementType
  title           String
  body            String
  imageUrl        String?               @map("image_url")
  priority        AnnouncementPriority  @default(normal)
  ctaType         AnnouncementCtaType   @default(none) @map("cta_type")
  ctaLabel        String?               @map("cta_label")
  ctaValue        String?               @map("cta_value")
  targetApps      String[]              @default([]) @map("target_apps")
  targetCountries String[]              @default([]) @map("target_countries")
  targetTiers     String[]              @default([]) @map("target_tiers")
  startsAt        DateTime?             @map("starts_at")
  endsAt          DateTime?             @map("ends_at")
  isActive        Boolean               @default(true) @map("is_active")
  createdBy       String?               @map("created_by") @db.Uuid
  createdAt       DateTime              @default(now()) @map("created_at")
  updatedAt       DateTime              @updatedAt @map("updated_at")
  reads           AnnouncementRead[]

  @@index([isActive, startsAt, endsAt])
  @@map("announcements")
}

model AnnouncementRead {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  announcementId String       @map("announcement_id") @db.Uuid
  userId         String       @map("user_id") @db.Uuid
  seenAt         DateTime     @default(now()) @map("seen_at")
  announcement   Announcement @relation(fields: [announcementId], references: [id], onDelete: Cascade)

  @@unique([announcementId, userId])
  @@map("announcement_reads")
}
```

- [ ] **Step 2 : Générer le client + pousser le schéma**

Run : `npx prisma generate && npx prisma db push`
Expected : `Your database is now in sync with your Prisma schema.` et le client régénéré sans erreur.

- [ ] **Step 3 : Commit**

```bash
git add prisma/schema.prisma
git commit -m "feat(annonces): modèles Announcement + AnnouncementRead"
```

---

### Task 2 : Permission RBAC `manage_announcements`

**Files:**
- Modify: `prisma/seed-rbac.ts`

- [ ] **Step 1 : Ajouter la permission dans le tableau PERMISSIONS**

Dans `prisma/seed-rbac.ts`, dans le `const PERMISSIONS`, ajouter une entrée (à la suite du groupe promos par cohérence) :

```typescript
  { key: 'manage_announcements',      group: 'announcements', description: 'Gérer les annonces et la version des apps' },
```

- [ ] **Step 2 : L'attribuer aux rôles admin/super_admin**

Repérer dans le même fichier la matrice rôle→permissions (objet associant chaque rôle à ses clés). Ajouter `'manage_announcements'` à la liste des permissions de `super_admin` ET `admin`. (Si `super_admin` reçoit `'*'` ou toutes les permissions automatiquement, n'ajouter qu'à `admin`.)

- [ ] **Step 3 : Rejouer le seed RBAC**

Run : `npx ts-node prisma/seed-rbac.ts` (ou la commande de seed du projet)
Expected : log indiquant la permission `manage_announcements` créée/à jour, sans erreur.

- [ ] **Step 4 : Commit**

```bash
git add prisma/seed-rbac.ts
git commit -m "feat(annonces): permission RBAC manage_announcements"
```

---

### Task 3 : DTOs

**Files:**
- Create: `src/announcements/dto/create-announcement.dto.ts`
- Create: `src/announcements/dto/update-announcement.dto.ts`
- Create: `src/announcements/dto/app-version.dto.ts`

- [ ] **Step 1 : CreateAnnouncementDto**

`src/announcements/dto/create-announcement.dto.ts` :

```typescript
import { IsString, IsOptional, IsEnum, IsArray, IsBoolean, IsDateString } from 'class-validator';

export class CreateAnnouncementDto {
  @IsEnum(['promo', 'info', 'ad']) type: 'promo' | 'info' | 'ad';
  @IsString() title: string;
  @IsString() body: string;
  @IsOptional() @IsString() imageUrl?: string;
  @IsOptional() @IsEnum(['high', 'normal']) priority?: 'high' | 'normal';
  @IsOptional() @IsEnum(['none', 'screen', 'promo_code']) ctaType?: 'none' | 'screen' | 'promo_code';
  @IsOptional() @IsString() ctaLabel?: string;
  @IsOptional() @IsString() ctaValue?: string;
  @IsOptional() @IsArray() targetApps?: string[];
  @IsOptional() @IsArray() targetCountries?: string[];
  @IsOptional() @IsArray() targetTiers?: string[];
  @IsOptional() @IsDateString() startsAt?: string;
  @IsOptional() @IsDateString() endsAt?: string;
  @IsOptional() @IsBoolean() isActive?: boolean;
}
```

- [ ] **Step 2 : UpdateAnnouncementDto**

`src/announcements/dto/update-announcement.dto.ts` :

```typescript
import { PartialType } from '@nestjs/mapped-types';
import { CreateAnnouncementDto } from './create-announcement.dto';

export class UpdateAnnouncementDto extends PartialType(CreateAnnouncementDto) {}
```

- [ ] **Step 3 : AppVersionDto**

`src/announcements/dto/app-version.dto.ts` :

```typescript
import { IsString, IsOptional } from 'class-validator';

export class AppVersionDto {
  @IsOptional() @IsString() min_version_passenger?: string;
  @IsOptional() @IsString() latest_version_passenger?: string;
  @IsOptional() @IsString() apk_url_passenger?: string;
  @IsOptional() @IsString() min_version_driver?: string;
  @IsOptional() @IsString() latest_version_driver?: string;
  @IsOptional() @IsString() apk_url_driver?: string;
}
```

- [ ] **Step 4 : Commit**

```bash
git add src/announcements/dto
git commit -m "feat(annonces): DTOs create/update/version"
```

---

### Task 4 : Service — filtrage du feed (TDD)

**Files:**
- Create: `src/announcements/announcements.service.ts`
- Test: `src/announcements/announcements.service.spec.ts`

- [ ] **Step 1 : Écrire le test du filtrage `matchesUser`**

`src/announcements/announcements.service.spec.ts` :

```typescript
import { AnnouncementsService } from './announcements.service';

describe('AnnouncementsService.matchesUser (filtrage)', () => {
  const svc = new AnnouncementsService({} as any, {} as any, {} as any);

  const base = {
    targetApps: [], targetCountries: [], targetTiers: [],
  } as any;

  it('filtre vide → match tout le monde', () => {
    expect(svc.matchesUser(base, { app: 'passenger', country: 'CM', tier: 'gold' })).toBe(true);
  });

  it('targetApps=[driver] → ne match pas un passager', () => {
    expect(svc.matchesUser({ ...base, targetApps: ['driver'] }, { app: 'passenger', country: 'CM', tier: 'gold' })).toBe(false);
  });

  it('targetCountries=[CM] → ne match pas TD', () => {
    expect(svc.matchesUser({ ...base, targetCountries: ['CM'] }, { app: 'passenger', country: 'TD', tier: null })).toBe(false);
  });

  it('targetTiers=[gold] ignoré pour app chauffeur', () => {
    expect(svc.matchesUser({ ...base, targetTiers: ['gold'] }, { app: 'driver', country: 'CM', tier: null })).toBe(false);
  });

  it('targetTiers=[gold] → match passager gold', () => {
    expect(svc.matchesUser({ ...base, targetTiers: ['gold'] }, { app: 'passenger', country: 'CM', tier: 'gold' })).toBe(true);
  });
});
```

- [ ] **Step 2 : Lancer le test (échec attendu)**

Run : `npx jest src/announcements/announcements.service.spec.ts -t matchesUser`
Expected : FAIL — `Cannot find module './announcements.service'`.

- [ ] **Step 3 : Implémenter le service avec `matchesUser`**

`src/announcements/announcements.service.ts` :

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../database/prisma.service';
import { SettingsService } from '../settings/settings.service';
import { UsersService } from '../users/users.service';

export type FeedUserCtx = { app: 'passenger' | 'driver'; country: string | null; tier: string | null };

@Injectable()
export class AnnouncementsService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly settings: SettingsService,
    private readonly users: UsersService,
  ) {}

  /** Vrai si l'annonce cible cet utilisateur. Filtres vides = pas de restriction. */
  matchesUser(a: { targetApps: string[]; targetCountries: string[]; targetTiers: string[] }, ctx: FeedUserCtx): boolean {
    if (a.targetApps.length && !a.targetApps.includes(ctx.app)) return false;
    if (a.targetCountries.length) {
      if (!ctx.country || !a.targetCountries.map(c => c.toUpperCase()).includes(ctx.country.toUpperCase())) return false;
    }
    if (a.targetTiers.length) {
      // Le tier n'existe que pour les passagers — une annonce tier-ciblée n'atteint pas les chauffeurs
      if (ctx.app !== 'passenger') return false;
      if (!ctx.tier || !a.targetTiers.includes(ctx.tier)) return false;
    }
    return true;
  }
}
```

- [ ] **Step 4 : Lancer le test (succès attendu)**

Run : `npx jest src/announcements/announcements.service.spec.ts -t matchesUser`
Expected : PASS (5 tests).

- [ ] **Step 5 : Commit**

```bash
git add src/announcements/announcements.service.ts src/announcements/announcements.service.spec.ts
git commit -m "feat(annonces): service + filtrage matchesUser (TDD)"
```

---

### Task 5 : Service — feed, seen, CRUD, version

**Files:**
- Modify: `src/announcements/announcements.service.ts`

- [ ] **Step 1 : Ajouter les méthodes feed/seen/CRUD/version**

Ajouter dans la classe `AnnouncementsService` :

```typescript
  /** Feed filtré pour un utilisateur : annonces actives dans la fenêtre + politique version. */
  async getFeed(userId: string, app: 'passenger' | 'driver') {
    const now = new Date();
    const profile = await this.users.findById(userId).catch(() => null);
    const country = (profile as any)?.countryCode ?? null;
    const tier = app === 'passenger' ? await this.users.getPassengerTier(userId).catch(() => null) : null;
    const ctx: FeedUserCtx = { app, country, tier };

    const candidates = await this.prisma.announcement.findMany({
      where: {
        isActive: true,
        AND: [
          { OR: [{ startsAt: null }, { startsAt: { lte: now } }] },
          { OR: [{ endsAt: null }, { endsAt: { gte: now } }] },
        ],
      },
      orderBy: { createdAt: 'desc' },
      include: { reads: { where: { userId }, select: { id: true } } },
    });

    const announcements = candidates
      .filter(a => this.matchesUser(a, ctx))
      .map(a => ({
        id: a.id, type: a.type, title: a.title, body: a.body, imageUrl: a.imageUrl,
        priority: a.priority, ctaType: a.ctaType, ctaLabel: a.ctaLabel, ctaValue: a.ctaValue,
        createdAt: a.createdAt, seen: a.reads.length > 0,
      }));

    const suffix = app;
    const version = {
      min: await this.settings.get(`min_version_${suffix}`, '0.0.0'),
      latest: await this.settings.get(`latest_version_${suffix}`, '0.0.0'),
      apkUrl: await this.settings.get(`apk_url_${suffix}`, ''),
    };

    return { announcements, version };
  }

  /** Marque une annonce comme vue (idempotent). */
  async markSeen(userId: string, announcementId: string) {
    await this.prisma.announcementRead.upsert({
      where: { announcementId_userId: { announcementId, userId } },
      update: {},
      create: { announcementId, userId },
    });
    return { ok: true };
  }

  // ── Admin ──
  listAdmin() {
    return this.prisma.announcement.findMany({ orderBy: { createdAt: 'desc' } });
  }

  create(dto: any, adminId: string) {
    return this.prisma.announcement.create({
      data: {
        ...dto,
        startsAt: dto.startsAt ? new Date(dto.startsAt) : null,
        endsAt: dto.endsAt ? new Date(dto.endsAt) : null,
        createdBy: adminId,
      },
    });
  }

  async update(id: string, dto: any) {
    const exists = await this.prisma.announcement.findUnique({ where: { id } });
    if (!exists) throw new NotFoundException('Annonce introuvable');
    return this.prisma.announcement.update({
      where: { id },
      data: {
        ...dto,
        ...(dto.startsAt !== undefined ? { startsAt: dto.startsAt ? new Date(dto.startsAt) : null } : {}),
        ...(dto.endsAt !== undefined ? { endsAt: dto.endsAt ? new Date(dto.endsAt) : null } : {}),
      },
    });
  }

  remove(id: string) {
    return this.prisma.announcement.delete({ where: { id } });
  }

  async stats(id: string) {
    const reads = await this.prisma.announcementRead.count({ where: { announcementId: id } });
    return { views: reads };
  }

  async getVersionConfig() {
    const keys = ['min_version_passenger','latest_version_passenger','apk_url_passenger','min_version_driver','latest_version_driver','apk_url_driver'];
    const out: Record<string, string> = {};
    for (const k of keys) out[k] = await this.settings.get(k, '');
    return out;
  }

  async setVersionConfig(dto: Record<string, string | undefined>) {
    for (const [k, v] of Object.entries(dto)) {
      if (v !== undefined) await this.settings.set(k, v);
    }
    return this.getVersionConfig();
  }
```

- [ ] **Step 2 : Vérifier que ça compile**

Run : `npx tsc --noEmit`
Expected : 0 erreur. (Si `users.findById` n'existe pas, utiliser la vraie méthode de `UsersService` qui renvoie le profil avec `countryCode` — adapter le nom.)

- [ ] **Step 3 : Commit**

```bash
git add src/announcements/announcements.service.ts
git commit -m "feat(annonces): feed filtré, markSeen, CRUD, config version"
```

---

### Task 6 : Controllers (mobile + admin) + module

**Files:**
- Create: `src/announcements/announcements.controller.ts`
- Create: `src/announcements/announcements.admin.controller.ts`
- Create: `src/announcements/announcements.module.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1 : Controller mobile**

`src/announcements/announcements.controller.ts` :

```typescript
import { Controller, Get, Post, Param, Query, UseGuards, Request } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards';
import { AnnouncementsService } from './announcements.service';

@Controller('announcements')
@UseGuards(JwtAuthGuard)
export class AnnouncementsController {
  constructor(private readonly svc: AnnouncementsService) {}

  @Get('feed')
  feed(@Request() req: any, @Query('app') app: 'passenger' | 'driver') {
    const resolved = app === 'driver' ? 'driver' : 'passenger';
    return this.svc.getFeed(req.user.id, resolved);
  }

  @Post(':id/seen')
  seen(@Request() req: any, @Param('id') id: string) {
    return this.svc.markSeen(req.user.id, id);
  }
}
```

- [ ] **Step 2 : Controller admin**

`src/announcements/announcements.admin.controller.ts` :

```typescript
import { Controller, Get, Post, Patch, Delete, Param, Body, UseGuards, Request } from '@nestjs/common';
import { JwtAuthGuard, RolesGuard } from '../auth/guards';
import { PermissionsGuard } from '../rbac/permissions.guard';
import { RequirePermission } from '../rbac/require-permission.decorator';
import { AnnouncementsService } from './announcements.service';
import { CreateAnnouncementDto } from './dto/create-announcement.dto';
import { UpdateAnnouncementDto } from './dto/update-announcement.dto';
import { AppVersionDto } from './dto/app-version.dto';

@Controller('admin/announcements')
@UseGuards(JwtAuthGuard, RolesGuard, PermissionsGuard)
@RequirePermission('manage_announcements')
export class AnnouncementsAdminController {
  constructor(private readonly svc: AnnouncementsService) {}

  @Get() list() { return this.svc.listAdmin(); }
  @Post() create(@Body() dto: CreateAnnouncementDto, @Request() req: any) { return this.svc.create(dto, req.user.id); }
  @Patch(':id') update(@Param('id') id: string, @Body() dto: UpdateAnnouncementDto) { return this.svc.update(id, dto); }
  @Delete(':id') remove(@Param('id') id: string) { return this.svc.remove(id); }
  @Get(':id/stats') stats(@Param('id') id: string) { return this.svc.stats(id); }
}
```

Et la config version dans un second controller pour la route `admin/app-version`. Ajouter dans le même fichier :

```typescript
@Controller('admin/app-version')
@UseGuards(JwtAuthGuard, RolesGuard, PermissionsGuard)
@RequirePermission('manage_announcements')
export class AppVersionAdminController {
  constructor(private readonly svc: AnnouncementsService) {}
  @Get() get() { return this.svc.getVersionConfig(); }
  @Patch() set(@Body() dto: AppVersionDto) { return this.svc.setVersionConfig(dto as any); }
}
```

- [ ] **Step 3 : Module**

`src/announcements/announcements.module.ts` :

```typescript
import { Module } from '@nestjs/common';
import { PrismaModule } from '../database/prisma.module';
import { SettingsModule } from '../settings/settings.module';
import { UsersModule } from '../users/users.module';
import { RbacModule } from '../rbac/rbac.module';
import { AnnouncementsService } from './announcements.service';
import { AnnouncementsController } from './announcements.controller';
import { AnnouncementsAdminController, AppVersionAdminController } from './announcements.admin.controller';

@Module({
  imports: [PrismaModule, SettingsModule, UsersModule, RbacModule],
  controllers: [AnnouncementsController, AnnouncementsAdminController, AppVersionAdminController],
  providers: [AnnouncementsService],
})
export class AnnouncementsModule {}
```

(Vérifier les noms exacts des modules importés : `PrismaModule`, `SettingsModule`, `UsersModule`, `RbacModule` — adapter si différents.)

- [ ] **Step 4 : Enregistrer dans AppModule**

Dans `src/app.module.ts`, ajouter l'import et `AnnouncementsModule` au tableau `imports`.

- [ ] **Step 5 : Compiler + démarrer**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 6 : Commit**

```bash
git add src/announcements src/app.module.ts
git commit -m "feat(annonces): controllers mobile+admin, module, routes version"
```

---

# PHASE 2 — ADMIN

### Task 7 : Méthodes API admin

**Files:**
- Modify: `src/services/api.ts`

- [ ] **Step 1 : Ajouter les méthodes**

Dans la classe API admin, ajouter :

```typescript
  async listAnnouncements() { return this.request<any[]>('/admin/announcements'); }
  async createAnnouncement(data: any) { return this.request('/admin/announcements', { method: 'POST', body: data }); }
  async updateAnnouncement(id: string, data: any) { return this.request(`/admin/announcements/${id}`, { method: 'PATCH', body: data }); }
  async deleteAnnouncement(id: string) { return this.request(`/admin/announcements/${id}`, { method: 'DELETE' }); }
  async getAnnouncementStats(id: string) { return this.request<{ views: number }>(`/admin/announcements/${id}/stats`); }
  async getAppVersion() { return this.request<Record<string,string>>('/admin/app-version'); }
  async setAppVersion(data: Record<string,string>) { return this.request('/admin/app-version', { method: 'PATCH', body: data }); }
```

(Adapter la signature `request` au helper existant — vérifier comment `setSetting` est écrit dans ce fichier et copier le style.)

- [ ] **Step 2 : Compiler**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 3 : Commit**

```bash
git add src/services/api.ts
git commit -m "feat(annonces): méthodes API admin"
```

---

### Task 8 : Page AnnoncesPage + route + sidebar

**Files:**
- Create: `src/pages/AnnoncesPage.tsx`
- Modify: `src/App.tsx`, `src/components/Sidebar.tsx`

- [ ] **Step 1 : Créer la page (2 onglets : Annonces / Version)**

`src/pages/AnnoncesPage.tsx` — structure calquée sur `PromosPage.tsx` :
- État : liste annonces (`adminApi.listAnnouncements`), pays dynamiques (`adminApi` route `/zones/admin/countries`), onglet actif.
- Liste avec badge type/statut + bouton Nouvelle.
- Éditeur (modal) : champs type, titre, body, imageUrl (upload via endpoint uploads existant), priorité, ctaType+ctaValue, multi-select targetApps/targetCountries/targetTiers, startsAt/endsAt, isActive.
- Onglet Version : 6 champs (`getAppVersion`/`setAppVersion`).
- Aperçu live du modal à droite de l'éditeur.

Suivre exactement les conventions visuelles de `PromosPage.tsx` (composants, classes Tailwind, toasts).

- [ ] **Step 2 : Ajouter la route**

Dans `src/App.tsx` :

```tsx
import { AnnoncesPage } from './pages/AnnoncesPage';
// ...
<Route path="/annonces" element={<PermissionRoute permission="manage_announcements"><AnnoncesPage /></PermissionRoute>} />
```

- [ ] **Step 3 : Ajouter l'entrée sidebar**

Dans `src/components/Sidebar.tsx`, importer `Megaphone` de lucide-react et ajouter :

```tsx
{ path: '/annonces', label: 'Annonces', icon: Megaphone, section: 'admin', permission: 'manage_announcements' },
```

- [ ] **Step 4 : Compiler + build**

Run : `npx tsc --noEmit && npm run build`
Expected : build réussi.

- [ ] **Step 5 : Commit**

```bash
git add src/pages/AnnoncesPage.tsx src/App.tsx src/components/Sidebar.tsx
git commit -m "feat(annonces): page admin AnnoncesPage + route + sidebar"
```

---

# PHASE 3 — MOBILE (passager d'abord)

### Task 9 : Service API + store annonces

**Files:**
- Modify: `services/api.ts`
- Create: `stores/announcementsStore.ts`

- [ ] **Step 1 : Méthodes API**

Dans `services/api.ts` (passager) :

```typescript
  async getAnnouncementsFeed(token: string) {
    return this.request<{
      announcements: { id: string; type: string; title: string; body: string; imageUrl: string|null; priority: 'high'|'normal'; ctaType: 'none'|'screen'|'promo_code'; ctaLabel: string|null; ctaValue: string|null; createdAt: string; seen: boolean }[];
      version: { min: string; latest: string; apkUrl: string };
    }>(`/announcements/feed?app=passenger`, { token });
  }
  async markAnnouncementSeen(token: string, id: string) {
    return this.request(`/announcements/${id}/seen`, { method: 'POST', token });
  }
```

(Adapter `app=passenger` → `app=driver` dans l'app chauffeur.)

- [ ] **Step 2 : Store avec cache 5 min**

`stores/announcementsStore.ts` :

```typescript
import { create } from 'zustand';
import { api } from '../services/api';

type Announcement = Awaited<ReturnType<typeof api.getAnnouncementsFeed>>['announcements'][number];
type Version = { min: string; latest: string; apkUrl: string };

interface State {
  announcements: Announcement[];
  version: Version | null;
  lastFetch: number;
  fetchFeed: (token: string) => Promise<void>;
  markSeen: (token: string, id: string) => void;
}

const CACHE_MS = 5 * 60 * 1000;

export const useAnnouncementsStore = create<State>((set, get) => ({
  announcements: [],
  version: null,
  lastFetch: 0,
  fetchFeed: async (token) => {
    if (Date.now() - get().lastFetch < CACHE_MS && get().version) return;
    try {
      const res = await api.getAnnouncementsFeed(token);
      set({ announcements: res.announcements, version: res.version, lastFetch: Date.now() });
    } catch { /* garder le cache existant */ }
  },
  markSeen: (token, id) => {
    set((s) => ({ announcements: s.announcements.map(a => a.id === id ? { ...a, seen: true } : a) }));
    api.markAnnouncementSeen(token, id).catch(() => {});
  },
}));
```

- [ ] **Step 3 : Compiler**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 4 : Commit**

```bash
git add services/api.ts stores/announcementsStore.ts
git commit -m "feat(annonces): service feed + store mobile (cache 5min)"
```

---

### Task 10 : Modal au lancement

**Files:**
- Create: `components/AnnouncementModal.tsx`
- Modify: `app/_layout.tsx`

- [ ] **Step 1 : Composant modal**

`components/AnnouncementModal.tsx` : affiche la 1ʳᵉ annonce `priority==='high' && !seen`. Image + titre + body + bouton CTA (selon ctaType : `screen` → `router.push(ctaValue)`, `promo_code` → `Clipboard.setStringAsync(ctaValue)` + toast, `none` → "OK"). Bouton fermer → `markSeen`.

```tsx
import { Modal, View, Text, Image, Pressable } from 'react-native';
import * as Clipboard from 'expo-clipboard';
import { router } from 'expo-router';
import Toast from 'react-native-toast-message';
import { useAnnouncementsStore } from '../stores/announcementsStore';
import { useAuthStore } from '../stores/authStore';

export function AnnouncementModal() {
  const token = useAuthStore(s => s.token);
  const { announcements, markSeen } = useAnnouncementsStore();
  const current = announcements.find(a => a.priority === 'high' && !a.seen);
  if (!current || !token) return null;

  const onCta = () => {
    if (current.ctaType === 'screen' && current.ctaValue) router.push(current.ctaValue as never);
    else if (current.ctaType === 'promo_code' && current.ctaValue) {
      Clipboard.setStringAsync(current.ctaValue);
      Toast.show({ type: 'success', text1: 'Code copié', text2: current.ctaValue });
    }
    markSeen(token, current.id);
  };

  return (
    <Modal transparent animationType="fade" visible>
      <View style={{ flex:1, backgroundColor:'#0008', justifyContent:'center', padding:24 }}>
        <View style={{ backgroundColor:'#fff', borderRadius:20, overflow:'hidden' }}>
          {current.imageUrl ? <Image source={{ uri: current.imageUrl }} style={{ width:'100%', height:160 }} /> : null}
          <View style={{ padding:20, gap:10 }}>
            <Text style={{ fontSize:18, fontWeight:'800' }}>{current.title}</Text>
            <Text style={{ fontSize:14, color:'#444' }}>{current.body}</Text>
            {current.ctaType !== 'none' && (
              <Pressable onPress={onCta} style={{ backgroundColor:'#1a56db', borderRadius:12, padding:12, alignItems:'center' }}>
                <Text style={{ color:'#fff', fontWeight:'700' }}>{current.ctaLabel ?? 'Voir'}</Text>
              </Pressable>
            )}
            <Pressable onPress={() => markSeen(token, current.id)} style={{ padding:8, alignItems:'center' }}>
              <Text style={{ color:'#888' }}>Fermer</Text>
            </Pressable>
          </View>
        </View>
      </View>
    </Modal>
  );
}
```

- [ ] **Step 2 : Monter + déclencher le fetch dans `_layout.tsx`**

Dans `app/_layout.tsx` : importer `useAnnouncementsStore`, `AppState`, et au montage + sur `AppState` 'active', appeler `fetchFeed(token)`. Rendre `<AnnouncementModal />` au niveau racine (après le Stack). Installer `expo-clipboard` si absent : `npx expo install expo-clipboard`.

- [ ] **Step 3 : Compiler**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 4 : Commit**

```bash
git add components/AnnouncementModal.tsx app/_layout.tsx package.json
git commit -m "feat(annonces): modal au lancement + fetch feed (launch+foreground)"
```

---

### Task 11 : Fusion dans le centre de notifications

**Files:**
- Modify: `app/(tabs)/notifications.tsx`

- [ ] **Step 1 : Fusionner annonces + push dans l'inbox**

Dans `notifications.tsx`, onglet "inbox" : construire une liste fusionnée triée par date à partir de `notificationInboxStore.items` (push locaux) et `useAnnouncementsStore().announcements` (mappés en items : `{ id, title, body, receivedAt: createdAt, read: seen, kind:'announcement' }`). Au tap sur une annonce → `markSeen` + exécuter le CTA (même logique que le modal). Le badge non-lu additionne `notificationInboxStore.unreadCount()` + nb annonces `!seen`.

- [ ] **Step 2 : Compiler + vérifier visuellement**

Run : `npx tsc --noEmit`
Expected : 0 erreur. Vérifier dans l'app que les annonces apparaissent dans l'inbox aux côtés des push.

- [ ] **Step 3 : Commit**

```bash
git add app/(tabs)/notifications.tsx
git commit -m "feat(annonces): fusion annonces dans le centre de notifs"
```

---

### Task 12 : Gate de version + téléchargement/installation APK in-app

**Files:**
- Create: `components/UpdateGate.tsx`
- Modify: `app/_layout.tsx`, `app.config.js`, `package.json`

- [ ] **Step 1 : Installer les dépendances**

Run : `npx expo install expo-file-system expo-intent-launcher`
Expected : ajoutées à `package.json`.

- [ ] **Step 2 : Permission Android**

Dans `app.config.js`, ajouter à `android.permissions` : `"REQUEST_INSTALL_PACKAGES"`.

- [ ] **Step 3 : Helper de comparaison SemVer**

Dans `UpdateGate.tsx`, fonction pure :

```typescript
function cmpVersion(a: string, b: string): number {
  const pa = a.split('.').map(n => parseInt(n, 10) || 0);
  const pb = b.split('.').map(n => parseInt(n, 10) || 0);
  for (let i = 0; i < 3; i++) { if ((pa[i]||0) !== (pb[i]||0)) return (pa[i]||0) - (pb[i]||0); }
  return 0;
}
```

- [ ] **Step 4 : Composant UpdateGate**

`components/UpdateGate.tsx` : lit `version` du store + `Application.nativeApplicationVersion` (expo-application). Si `current < min` → écran bloquant. Si `current < latest` → bandeau fermable. Bouton "Mettre à jour" → télécharge `apkUrl` via `FileSystem.createDownloadResumable` (callback progression %), puis `IntentLauncher.startActivityAsync('android.intent.action.INSTALL_PACKAGE'...)` ou ouverture du fichier via `IntentLauncher` avec `contentUri`.

```tsx
import { useState } from 'react';
import { View, Text, Pressable, Platform } from 'react-native';
import * as FileSystem from 'expo-file-system';
import * as IntentLauncher from 'expo-intent-launcher';
import * as Application from 'expo-application';
import { useAnnouncementsStore } from '../stores/announcementsStore';

export function UpdateGate({ children }: { children: React.ReactNode }) {
  const { version } = useAnnouncementsStore();
  const [progress, setProgress] = useState<number | null>(null);
  const [dismissed, setDismissed] = useState(false);
  const current = Application.nativeApplicationVersion ?? '0.0.0';

  if (!version) return <>{children}</>;
  const belowMin = cmpVersion(current, version.min) < 0;
  const belowLatest = cmpVersion(current, version.latest) < 0;

  const download = async () => {
    if (!version.apkUrl || Platform.OS !== 'android') return;
    const dest = FileSystem.cacheDirectory + 'update.apk';
    const dl = FileSystem.createDownloadResumable(version.apkUrl, dest, {}, (p) => {
      setProgress(p.totalBytesWritten / p.totalBytesExpectedToWrite);
    });
    const res = await dl.downloadAsync();
    if (res?.uri) {
      const contentUri = await FileSystem.getContentUriAsync(res.uri);
      await IntentLauncher.startActivityAsync('android.intent.action.INSTALL_PACKAGE', {
        data: contentUri, flags: 1,
      });
    }
    setProgress(null);
  };

  if (belowMin) {
    return (
      <View style={{ flex:1, justifyContent:'center', alignItems:'center', padding:24, gap:16 }}>
        <Text style={{ fontSize:18, fontWeight:'800' }}>Mise à jour requise</Text>
        <Text style={{ textAlign:'center', color:'#555' }}>Votre version est trop ancienne. Mettez à jour pour continuer.</Text>
        {progress !== null
          ? <Text>Téléchargement… {Math.round(progress*100)}%</Text>
          : <Pressable onPress={download} style={{ backgroundColor:'#1a56db', padding:14, borderRadius:12 }}>
              <Text style={{ color:'#fff', fontWeight:'700' }}>Mettre à jour</Text>
            </Pressable>}
      </View>
    );
  }

  return (
    <>
      {belowLatest && !dismissed && (
        <View style={{ backgroundColor:'#FFF8E1', padding:12, flexDirection:'row', alignItems:'center', gap:8 }}>
          <Text style={{ flex:1, color:'#7B5E00' }}>Une mise à jour est disponible.</Text>
          <Pressable onPress={download}><Text style={{ color:'#1a56db', fontWeight:'700' }}>Installer</Text></Pressable>
          <Pressable onPress={() => setDismissed(true)}><Text style={{ color:'#999' }}>✕</Text></Pressable>
        </View>
      )}
      {children}
    </>
  );
}
```

Installer `expo-application` si absent : `npx expo install expo-application`.

- [ ] **Step 5 : Envelopper l'app**

Dans `app/_layout.tsx`, envelopper le Stack avec `<UpdateGate>…</UpdateGate>` (après que le feed soit fetché).

- [ ] **Step 6 : Compiler**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 7 : Commit**

```bash
git add components/UpdateGate.tsx app/_layout.tsx app.config.js package.json
git commit -m "feat(annonces): gate version + téléchargement/installation APK in-app"
```

---

# PHASE 4 — MOBILE CHAUFFEUR

### Task 13 : Répliquer Phase 3 dans l'app chauffeur

**Files:**
- Create/Modify: équivalents dans `aerocab-driver` (`services/api.ts`, `stores/announcementsStore.ts`, `components/AnnouncementModal.tsx`, `components/UpdateGate.tsx`, `app/_layout.tsx`, `app/(tabs)/notifications.tsx` si présent, `app.config.js`)

- [ ] **Step 1 : Copier les composants/stores**

Reprendre Tasks 9–12 à l'identique dans `aerocab-driver`, en changeant `app=passenger` → `app=driver` dans `getAnnouncementsFeed`. Le chauffeur n'a pas de centre de notifs séparé (Task 11 à adapter ou omettre selon l'existant — vérifier si `app/(tabs)/notifications.tsx` existe côté driver ; sinon, exposer les annonces via un écran simple ou seulement le modal + gate).

- [ ] **Step 2 : Permission Android driver**

Ajouter `REQUEST_INSTALL_PACKAGES` dans `aerocab-driver/app.config.js`.

- [ ] **Step 3 : Compiler**

Run : `npx tsc --noEmit` (dans aerocab-driver)
Expected : 0 erreur.

- [ ] **Step 4 : Commit**

```bash
git add -A
git commit -m "feat(annonces): intégration app chauffeur (modal + gate version)"
```

---

# PHASE 5 — VALIDATION END-TO-END

### Task 14 : Test manuel du flux complet

- [ ] **Step 1 : Backend** — créer une annonce `high` ciblée passager/CM via l'admin, vérifier `GET /announcements/feed?app=passenger` la renvoie avec `seen:false`, et qu'un `POST /:id/seen` la passe à `seen:true`.
- [ ] **Step 2 : Version** — régler `min_version_passenger` > version courante → l'app passager affiche l'écran bloquant ; régler `latest_version_passenger` > courante (et min ≤ courante) → bandeau fermable.
- [ ] **Step 3 : Ciblage** — créer une annonce `targetApps:['driver']` → absente du feed passager, présente du feed chauffeur.
- [ ] **Step 4 : Tier** — annonce `targetTiers:['gold']` → présente pour passager gold, absente pour bronze et pour chauffeur.
- [ ] **Step 5 : Déploiement backend VPS** — transférer les fichiers backend, `docker compose build api && up -d api`, vérifier `db push` a créé les tables (`announcements`, `announcement_reads`).

---

## Notes d'implémentation

- **Adapter les noms réels** : `UsersService.findById`/`getPassengerTier`, noms des modules (`PrismaModule`/`SettingsModule`/`UsersModule`/`RbacModule`), et le helper `request` de l'API admin — vérifier dans le code existant avant d'écrire, suivre les patterns en place.
- **`getContentUriAsync` + FileProvider** : `expo-file-system` fournit `getContentUriAsync` qui gère le `content://` requis par Android pour l'install. Si l'install échoue avec une erreur de permission fichier, vérifier qu'aucune config FileProvider manuelle n'est nécessaire (expo-file-system la fournit).
- **Pays dynamiques admin** : réutiliser l'endpoint `GET /zones/admin/countries`.
